import React from "react";
import styled from "styled-components";

const StickerCont = () => {
  return <StickerProp>{"= {)14"}</StickerProp>;
};

export default StickerCont;

const StickerProp = styled.div`
  color: black;
  margin: 0;
  padding: 0;
  font-size: 14px;
  overflow: hidden;
  position: relative;
`;
